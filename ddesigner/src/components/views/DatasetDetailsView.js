import React, { Component } from 'react';
import {translate} from "react-i18next";
import { Row, Col } from 'antd';
import resource from "../../Resource";

class DatasetDetailsView extends Component {
    constructor(...args) {
        super(...args);
    }

    render(){
        return(
            console.log('test')
        )
    }
}

export default DatasetDetailsView;
